// Rejected: the call cannot show size([]) > 0, so it fails spread's input contract.
fn spread(lst) [{
    define s = size(lst);
    rewrite eqToGtZero s;
    affirm s > 0;
}] {
    return last(lst) - first(lst);
};

return spread([]);
