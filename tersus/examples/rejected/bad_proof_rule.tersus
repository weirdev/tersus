// Rejected: a proof rule must establish its output (x = 0) from its body,
// and the body only shows x > 0.
proof positiveIsZero(x) [{
    affirm x > 0;
}] [{
    affirm x = 0;
}] {
    affirm x > 0;
};
