// Rejected: the guard says size(lst) > 1, which does not show the size(lst) > 0 that first needs.
fn firstOr(lst, fallback) {
    r = fallback;
    if size(lst) > 1 {
        r = first(lst);
    };
    return r;
};

return firstOr([4, 8], 0);
