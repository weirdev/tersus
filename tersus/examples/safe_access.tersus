fn spread(lst) [{
    define s = size(lst);
    rewrite eqToGtZero s;
    affirm s > 0;
}] [{
    affirm s > 0;
}] {
    return last(lst) - first(lst);
};

a = spread([4, 8, 15, 16, 23, 42]);
b = spread([7]);
return a + b;
